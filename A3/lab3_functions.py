import numpy as np

def ml_params(X, labels):
    """Computes the ML-estimates of micro_k and sigma_k for different classes in dataset X"""
    micro_k =
    sigma_k =

    return micro_k, sigma_k
    